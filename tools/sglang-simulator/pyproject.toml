[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sglang_simulator"
dynamic = ["version"]
description = "A simulation benchmark tool for sglang"
dependencies = []
